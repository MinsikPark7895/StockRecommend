# ============================================
# SECURITY WARNING: This is a template file!
# Copy this file to .env and set your actual values
# NEVER commit .env file to version control!
# ============================================

# PostgreSQL Database Configuration
# REQUIRED: Set strong passwords for production use
POSTGRES_DB=stockrecommend
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_THIS_TO_A_STRONG_PASSWORD
# SECURITY: In production, use non-standard ports to reduce port scanning risk
# Default ports (5432, 8000) are for local development convenience
POSTGRES_PORT=5432

# Application Configuration
APP_PORT=8000

# Database Connection URL
# REQUIRED: Update the password in the URL to match POSTGRES_PASSWORD above
# For local development (outside Docker): use localhost instead of postgres
# For Docker containers: use postgres (service name)
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
DATABASE_URL=postgresql://postgres:CHANGE_THIS_TO_A_STRONG_PASSWORD@postgres:5432/stockrecommend

# ============================================
# API Keys Configuration
# ============================================
# SECURITY: Never commit actual API keys to version control!
# Get your API key from: https://site.financialmodelingprep.com/developer/docs/
# Free tier: 250 requests/day, 5 requests/minute
FMP_API_KEY=your_fmp_api_key_here

# Additional API Keys (for future use)
# NEWS_API_KEY=your_api_key_here
# YFINANCE_API_KEY=your_api_key_here
